spring.application.name=project-management-tool-auth-service
spring.datasource.url=${DB_URL:jdbc:postgresql://host.docker.internal:5434/auth_service_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}

spring.datasource.driver-class-name=org.postgresql.Driver
server.port=${SERVER_PORT:8081}
spring.jpa.hibernate.ddl-auto=create-drop

# JWT Configuration - Must be 256-bit (32 characters) minimum
jwt.secret=${JWT_SECRET:dGhpcyBpcyBhIDI1NiBiaXQgc2VjdXJlIGtleSBmb3IgSldUIGF1dGhlbnRpY2F0aW9uIQ==}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:appt7637@gmail.com}
spring.mail.password=${MAIL_PASSWORD:rxpf xckq vwvj wtwn}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Swagger & API documentation
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html

# Email Verification Configuration
app.mail.from=${MAIL_FROM:noreply@projectmanagement.com}
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
app.email.verification.expiry-hours=24
